# 纬度：`<id>`

> 复制本文件为 `dimensions/<id>.md`。**先在 `gates.json` 里加门禁 id**，再写这里，然后 `npm run verify`。

## 一句话

**用现在时写：做对时系统必须出现什么可观察结果。**

## 何时用

- 什么阶段打开这份纬度
- 和哪些已有纬度一起用（不要重复它们的门禁）

## 验收句（必须能写成检查）

1. …
2. …

## 门禁

- [ ] `XXX-1` …
- [ ] `XXX-2` …

## 禁止

- 不要…
- 不要…

## 产出

过关时必须留下的证据：测试日志、对照表、灰度记录，而不是「感觉可以了」。
